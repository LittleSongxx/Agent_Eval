# Agent_Eval 项目审查与后续优化交接

> 交接日期：2026-08-19（Asia/Shanghai）
> 交接对象：下一位继续维护/优化 Agent_Eval 的 AI 代理
> 当前分支：`dev`
> 项目路径：`/home/song/code/Agent/eval/Agent_Eval`

## 0. 先读这里

本文件记录本轮对 Agent_Eval 的全面审查、已经取得的证据、尚未完成的工作和后续建议。它是工作交接记录，不是对项目所有问题的最终修复报告。

本轮用户任务是：

1. 全面分析 Agent_Eval 项目。
2. 阅读 `AI应用开发_Java后端_真实面试题与备考报告_20260819.md`，评估项目与 AI 应用/Agent/AI 后端/Java 后端等岗位能力的匹配程度。
3. 判断项目是否有清晰、可描述、可复现的闭环功能。
4. 判断项目是否功能完善，找出明显 Bug 和不合理设计。
5. 对相关技术和岗位要求做必要的联网调研。
6. 忽略未提交的删除文档对分析结论的影响。
7. 后续又明确要求：把本次任务、进度和后续任务写成 handoff，并将工作树当前变更连同 handoff 一起提交和推送。

重要口径：

- 现有 README、CHANGELOG、`docs/` 下的实验/健康检查报告和历史说明只当作待验证声明，不当作实现证据。用户已明确说明这些说明文档不一定和当前代码完全对齐。
- 结论优先依据当前 Python/TypeScript 代码、模型/API 调用链、测试、构建和可复跑命令。
- 本轮不恢复用户已有的删除文档。按用户最新“全部提交”指令，工作树中已有删除会随本次提交记录；下一位不要因为这些删除自动恢复文件。
- 不要把没有真实运行过的 LLM、被测接口、浏览器、生产部署或联网调研写成已验证事实。

## 1. 当前 Git 与交付状态

### 1.1 审查开始时的状态

- 分支：`dev`
- 上游：`origin/dev`
- 审查开始时 HEAD：`3ecfe4b feat: 完成秋招优化全部四大模块`
- 审查开始时工作树包含：
  - 一个新的、未跟踪的面试报告：`AI应用开发_Java后端_真实面试题与备考报告_20260819.md`
  - 26 个用户已有的删除文档变更，主要是旧版面试报告、旧 `docs/README`、旧阶段总结和旧优化报告。
- 本轮没有修改业务代码；本文件是本轮新增的唯一代码库文件。

### 1.2 提交边界

提交前必须再次确认暂存区：

```bash
git status --short --branch
git diff --cached --name-status
git diff --cached --stat
```

按用户最新要求，本次应使用 `git add -A` 纳入：

- `HANDOFF_20260819.md`
- 新的 `AI应用开发_Java后端_真实面试题与备考报告_20260819.md`
- 工作树中已有的删除文档。

以下文件必须保持未纳入提交（它们已被 `.gitignore` 忽略，仍需显式复核）：

- `backend/.env`、任何 `.env` 或 API Key 文件。
- `backend/*.db`、SQLite 备份、WAL/SHM 文件。
- `backend/uploads/`。
- `frontend/node_modules/`、`frontend/dist/`、TypeScript 缓存。

推荐提交命令：

```bash
git add -A
git diff --cached --check
git diff --cached --name-status
git commit -m "docs: add Agent_Eval audit handoff"
git push origin dev
```

若推送被远端保护规则拒绝，不要强推、不要 rebase 或 reset；保留本地提交并把远端错误完整记录给用户。

## 2. 项目定位与当前初步结论

### 2.1 项目实际定位

当前项目不是一个 RAG/Agent 业务应用本体，也不是 Java 后端服务。它是一个用 FastAPI + SQLAlchemy + SQLite/关系数据库 + React/Ant Design 构建的 AI 应用评测工作台，核心价值是把：

```text
数据集/样本
  -> 评测场景与指标
  -> Judge LLM 或被测接口调用
  -> 逐行评分、理由、trace、成本/延迟
  -> 报告与逐行复核
  -> 人工盲测、人工一致性与报告比较
```

串成一条可重复的本地评测流程。

### 2.2 闭环判断（初评）

在“可信内部用户、单进程/单机、本地 SQLite、评测任务创建后立即执行、已有 LLM Key”的前提下，主产品链条是成立的：

1. 配置 Judge LLM：`/api/llm-configs`。
2. 创建/导入数据集和数据行：`/api/datasets`。
3. 创建场景、指标、阈值、权重和 Prompt 覆盖：`/api/scenarios`、`/api/metrics`。
4. 创建离线评测或接口实时评测：`/api/evaluations`。
5. 后台线程执行逐行指标，写入 `EvalRowResult`，推送 WebSocket/轮询进度。
6. 生成报告摘要、指标均值/通过率、成本、延迟、Judge 稳定性和逐行理由。
7. 在报告详情页进行人工标注/仲裁，计算人工-自动和人-人一致性。
8. 对同一数据集/场景的两次任务做逐行与指标比较，并显式报告评测口径是否一致。
9. 另有独立人工盲测流程比较两个模型/接口。

因此，可以清晰描述为“评测运营闭环”或“AI 应用上线前后的质量验证工作台”。

但它目前不是完整的“质量持续改进闭环”：人工 Bad Case、标注、盲测结果不会自动生成回归数据集、自动修改 Prompt/指标、创建下一轮任务或形成发布门禁；最终闭环仍依赖人手动决策和再次操作。后续交接时不要把“报告有校准建议”说成“系统自动完成了优化闭环”。

### 2.3 岗位匹配初评（待联网核验后再定稿）

以下是基于报告题族和当前实现的临时分档，不是市场概率：

| 目标方向 | 初步匹配 | 可以用项目证明的内容 | 明显缺口 |
|---|---|---|---|
| AI 评测 / LLM-as-a-Judge / AI 平台 | 高 | 指标执行器、Judge 工程化、数据集版本、口径快照、人工校准、报告比较、成本/延迟 | 生产部署、权限、规模化任务编排、外部观测性 |
| AI 应用后端 / AI 全栈 | 中 | FastAPI API、React 工作台、RAG/Agent/多轮评测、SSE/WS、接口适配 | 没有被测业务 Agent/RAG 的运行时本体、鉴权、部署、线上用户/业务指标 |
| Agent/智能体开发 | 中偏低到中 | 工具调用数据、Agent 指标、失败样本、轨迹相关实验脚本 | 没有 LangGraph/MCP/Memory/Workflow 运行时主链，若干 Agent 研究指标未接入产品 |
| Python/FastAPI AI 后端 | 中高 | 异步 API、并发评测、取消、恢复、SQLAlchemy、HTTP/SSE 适配 | 任务队列、多进程/多实例、可观测性、认证和生产运维 |
| Java/Spring 后端 | 低到中 | 可讲数据模型、并发、任务状态、接口治理等通用后端设计 | 仓库没有 Java/Spring；没有 JVM、MySQL/Redis/Kafka、微服务、Java 线上证据 |
| AI Infra/模型训练/推理 | 低 | 仅能讲应用层评测和成本/延迟 | Transformer 训练、KV Cache、推理引擎、GPU/NPU、分布式推理均不是本项目主能力 |

项目面试开场应诚实使用“我做了一个 AI 应用评测平台”，不要包装成“我实现了一个多 Agent/RAG 产品”。

## 3. 本轮已完成的代码审查范围

### 3.1 后端主链

重点阅读了：

- `backend/app/main.py`：FastAPI lifespan、建表、运行时 schema 补列、种子数据、任务恢复、路由注册。
- `backend/app/api/evaluation.py`：任务创建、数据/场景/Judge 校验、快照、指纹、调试、取消、日志。
- `backend/app/core/evaluation_engine.py`：Judge 客户端、内置/自定义/确定性指标、复合指标、采样、多裁判、换序检查、并发执行、回写、摘要、恢复。
- `backend/app/api/report.py`：报告摘要、行结果、人工标注、kappa/一致性、比较。
- `backend/app/core/endpoint_eval.py`：JSON/SSE 被测接口调用、请求模板、字段映射、重试。
- `backend/app/core/blind_test_engine.py` 与 `backend/app/api/blind_test.py`：双目标答案生成、随机展示、人工投票。
- `backend/app/core/rag_dataset_generator.py` 与 `backend/app/api/rag_dataset_job.py`：文档上传、清洗、切块、生成问题/答案、人工挑选后生成评测数据集。
- `backend/app/core/scenario_snapshot.py`：场景、Judge、引擎 Prompt 摘要和评测指纹。
- `backend/app/core/annotation.py`、`agreement.py`：多标注者、仲裁和 Cohen kappa。

### 3.2 前端主链

当前路由集中在 `frontend/src/App.tsx`：

- `ExperimentWorkbenchPage.tsx`：工作台、调试、任务创建。
- `LLMConfigPage.tsx`：Judge 配置。
- `DatasetListPage.tsx`、`DatasetDetailPage.tsx`、`RagDatasetBuilderPage.tsx`：数据与 RAG 数据集生成。
- `MetricListPage.tsx`、`ScenarioListPage.tsx`：指标/场景。
- `EndpointTargetPage.tsx`：被测接口。
- `EvaluationPage.tsx`：执行、WS 实时进度、轮询降级、取消。
- `ReportListPage.tsx`、`ReportDetailPage.tsx`：摘要、逐行结果、可视化、人工复核、对比。
- `BlindTestPage.tsx`：盲测投票和汇总。

API 封装在 `frontend/src/services/api.ts`，但大量参数和返回值仍使用 `any`，前端没有独立单元/E2E 测试。

### 3.3 模型与持久化

主要模型位于 `backend/app/models/`：

- `Dataset` / `DatasetRow`：数据集、行数据、版本号。
- `MetricDefinition`、`EvalScenario`、`ScenarioMetric`：指标与场景配置。
- `LLMConfig`、`EndpointTarget`：Judge 与被测端配置。
- `EvalTask`、`EvalRowResult`、`RowAnnotation`：任务、逐行结果、多人标注。
- `BlindTestTask`、`BlindTestRowResult`：盲测。
- `RagDatasetJob` 及文档/Chunk/样本模型：RAG 数据集生产。

任务创建时保存 `scenario_snapshot`、`judge_snapshot`、`dataset_version` 和 `eval_fingerprint`，这是当前工程化设计中最有价值的部分之一。

## 4. 已验证的测试与构建证据

### 4.1 推荐环境

用户明确说明项目使用名为 `eval` 的 conda 环境。本轮后续验证已经切换到该环境：

```bash
conda run -n eval python -V
# Python 3.11.15
```

不要把之前误用 base 环境的结果当成最终环境结论。

### 4.2 后端

```bash
cd backend
conda run -n eval python -m pytest tests -q
```

结果：

- `328 passed`
- `1 warning`：Starlette 提示当前 `httpx` 与 `TestClient` 的组合将来需要迁移到 `httpx2`。

```bash
conda run -n eval python -m compileall -q app scripts
conda run -n eval python -c "from app.main import app; print(app.title, app.version, len(app.routes))"
```

结果：编译通过，应用可导入，打印 `AI Evaluation Platform 0.1.0 16`。

### 4.3 前端

```bash
cd frontend
npm run build
```

结果：TypeScript 和 Vite 构建通过，3691 个模块转换完成。构建警告：

- 主 JS bundle 约 `1,903.46 kB`，gzip 约 `588.92 kB`。
- 目前没有路由级动态 import/code splitting。

### 4.4 依赖检查

```bash
cd frontend
npm audit --omit=dev --json
```

当前锁定版本报告 5 个生产依赖安全告警：2 high、3 moderate，涉及 `axios`、`react-router`/`react-router-dom`、`@remix-run/router` 和传递依赖 `form-data`。下一位应先确认 advisory 对浏览器构建的实际可利用性，再升级并回归测试，不要只用 `npm audit fix --force`。

```bash
cd backend
conda run -n eval python -m pip check
```

唯一输出是环境中 `ipykernel 7.1.0 requires psutil`。这不是项目 `requirements.txt` 的应用依赖冲突，但说明共享 conda 环境并非完全干净；需要时补 `psutil` 或移除损坏的 notebook 依赖。当前 `requirements.txt` 都是宽范围版本，没有 Python lock/conda environment 文件，CI 也只按 `requirements.txt` 重装。

### 4.5 尚未验证的内容

以下内容本轮没有真实验证，后续不得引用为“已通过”：

- 使用真实 DashScope/OpenAI-compatible Key 的 Judge 全流程。
- 真实 RAG/Agent 被测 endpoint 的 JSON/SSE 联调。
- 浏览器在桌面/移动 viewport 的 Playwright 检查、截图和交互。
- 多进程 Uvicorn、多 worker、PostgreSQL、真实公网部署。
- 上传大型/恶意文档、超长 SSE、异常 HTTP 状态、网络断线和 SSRF 安全测试。
- 本轮任务要求的外部联网技术/JD 调研的独立复核。面试报告本身声称做过检索，但不能代替本轮对官方文档和来源的复核。

## 5. 当前能力分层：已经进入产品主链 vs 仅原型/脚本

### 5.1 已经进入产品主链的能力

- 数据集 CRUD、CSV/JSON 导入导出、行级校验和版本自增。
- RAG、Agent、多轮对话的预置指标/场景。
- OpenAI-compatible Judge 调用、结构化 JSON 评分、理由落库。
- RAG 断言级忠实度、生成式相关性、检索 HitRate@K、MRR 等部分复合/确定性指标。
- 离线已有结果评测。
- JSON/SSE 被测接口调用、请求体模板、响应字段映射、endpoint trace。
- 行级并发、任务取消、任务心跳、进程重启后的中断回收。
- 多采样标准差、换序一致性、多 Judge 面板 MAD/多数票。
- 场景快照、Judge 身份快照、数据集版本和评测指纹。
- 报告摘要、指标/延迟/成本、逐行 Judge 理由和错误。
- 多标注者独立标注、仲裁、人-人 kappa、Judge-人 kappa、置信区间和校准提示。
- 人工盲测：LLM/Endpoint 两端答案、随机左右顺序、人工投票。
- RAG 文档生成评测样本的任务型工作流。

### 5.2 有代码但没有进入完整产品主链的能力

这些模块不能在项目介绍中当作当前 Web 产品已交付能力，除非下一位完成接入并补测试：

- `TrajectoryFaithfulnessMetric`、`ErrorRecoveryMetric`、`ToolSelectionRationalityMetric`：定义在 `evaluation_engine.py`，目前主要由 `backend/scripts/agent_trajectory_experiment.py` 直接实例化；`build_metric()` 没有对应分派，预置指标/UI 也没有暴露。
- `CitationAccuracyMetric`：有执行器和实验脚本，但种子指标/场景未暴露；`build_metric()` 中相同 `metric_type == "builtin_citation_accuracy"` 分支重复出现。
- `adversarial_judge.py`：有多裁判/对抗分析类和测试，但没有 API/前端主流程入口。
- `explainable_evaluation.py`：有解释分析器和测试，没有报告 API/UI 接入证据。
- `cost_optimization.py`：主要被测试/实验引用，不是任务执行的自适应成本策略。
- `multimodal_client.py`、`multimodal_evaluator.py`：有实现和测试，但没有主 API 路由、场景、前端入口。
- 各种 `backend/scripts/*experiment*.py`、对比 Ragas/污染检测/黄金集脚本：属于研究和离线实验工具，不是平台用户点击即可复跑的主产品流程。

## 6. 已确认或高度可信的缺陷/设计问题

优先级只代表后续处理顺序，不等于安全漏洞 CVSS。下一位修复前应为每项补一个最小回归测试。

### P0：共享环境/真实数据前必须处理

#### P0-1 无认证、无 RBAC、无租户隔离

证据：`frontend/src/services/api.ts` 没有认证 header；`backend/app/main.py` 只注册 CORS，没有认证依赖；所有 CRUD、报告、标注和 endpoint 调用均可直接访问。后端默认绑定 `0.0.0.0`，CORS 不是服务端授权。

影响：任何能访问端口的人都可以读取/修改数据、调用已保存的接口、修改评测口径、标注结果或删除配置。面试报告中的“鉴权、租户隔离、工具权限”目前只能作为待建设项。

建议：先明确可信内网边界；若要对外或多人使用，增加身份认证、角色权限、租户字段和审计事件，标注者身份必须从会话取得，不能继续信任请求体里的 `annotator`。

#### P0-2 服务端可任意访问用户提供的 endpoint（SSRF/内网探测风险）

证据：`backend/app/core/endpoint_eval.py::invoke_endpoint` 和盲测 `EndpointResponseClient` 直接使用数据库/请求体里的 URL、Authorization 和自定义 headers，没有 host allowlist、私网地址阻断、DNS 重绑定防护或网络出口隔离。

影响：在非可信部署中，可访问云 metadata、内网管理端口或本机服务，并把响应带回报告。

建议：默认只允许显式登记且经过管理员批准的 HTTPS host；解析后阻断 loopback/link-local/private/multicast 地址，限制重定向和响应大小，必要时将 outbound worker 放入隔离网络。

#### P0-3 密钥和敏感响应虽有 API 脱敏，但存储/日志仍是明文

证据：`LLMConfig.api_key`、`EndpointTarget.authorization`、`EvalTask.target_config`、`BlindTestTask.target_a/target_b` 都会保存执行所需秘密；`OpenAIJudgeClient.judge_json` 在 INFO 日志打印完整 Judge messages；endpoint trace 保存 request body/raw response。

影响：日志、数据库备份和有读取权限的用户可取得 API Key、Authorization、Prompt 中的业务数据或个人信息。`schemas/sensitive.py` 只负责响应掩码，不是加密。

建议：外部 Secret Manager/KMS 或应用层加密；日志默认不打印完整 prompt/raw response，提供可控脱敏 debug 模式；对 trace 设置保留期、大小上限和字段级脱敏。

### P1：会影响结果正确性或可复现性

#### P1-1 endpoint `write_back` 没有递增数据集版本

证据：`evaluation_engine.py` 约 2074-2082 行在 `result_save_mode == "write_back"` 时直接修改 `dataset_row.data` 并补 schema，但没有调用 `backend/app/api/dataset.py::_bump_dataset_version`。

影响：被测接口提取出的 response/contexts 已改变数据集内容，但 `Dataset.version` 不变；下一次任务会复用旧版本号，`eval_fingerprint` 可能错误地认为输入数据未变，报告比较和历史追溯失真。

修法建议：把数据集行变更封装成统一 service，在事务中自增版本；为 endpoint write-back 增加“版本从 N 变 N+1、下一任务指纹变化”的回归测试。若不希望评测过程改变基准数据，默认应禁用 write-back，或将回写保存到独立输出表。

#### P1-2 Judge snapshot 不是实际执行配置的快照

证据：创建任务时 `build_judge_snapshot()` 只保存身份元数据；`run_evaluation()` 随后通过 `task.llm_config` 和 `task.judge_panel` ID 查询当前 `LLMConfig`。如果任务 pending、排队、服务重启或用户在执行期间修改配置，实际调用可能使用新 model/base URL/temperature，而 fingerprint 仍描述创建时配置。附加 panel 只保存 ID，不保存 panel 的 model/base URL/temperature。

影响：报告声称可追溯/可比较，但实际打分者可能和快照不一致；同一个 panel ID 改配置后，两个任务 fingerprint 仍可能相同。

修法建议：任务创建时冻结完整非秘密 Judge 配置，并让 worker 使用冻结值构造客户端；panel 也保存完整快照，fingerprint 纳入每个 panel 的实际身份。若保留数据库 ID，至少校验当前配置与快照不一致时任务失败并提示。

#### P1-3 SSE 解析没有可靠保留结构化上下文/工具调用

证据：`endpoint_eval.py::_invoke_sse` 将每个 `data:` payload 只提取成文本片段，最终 `parsed_response` 只有 `{"answer": ..., "events": [...]}`；原始 payload 的 `retrieved_contexts`、`tool_calls` 等结构不再作为结构化对象保留。`extract_eval_fields()` 再按 `response_mapping` 取这些字段时通常无法成功。

另外，Agent 确定性指标 `ToolCallAccuracyMetric`、`ArgumentCorrectnessMetric`、`StepEfficiencyMetric` 通过 `_extract_actual_tool_calls(row_data["user_input"])` 读取工具调用；endpoint 映射出来的顶层 `tool_calls` 没有合并进该位置。

影响：JSON endpoint 可能能评估 Agent 工具/RAG 字段，SSE endpoint 只得到 answer；README 所述“实时评测 RAG/Agent 接口”在常见流式协议下不完整。

修法建议：实现统一 SSE event parser，保留最终结构化事件/累积字段和 `[DONE]` 语义；定义 `endpoint_trace.extracted_fields` 的规范；Agent 指标接受显式 `tool_calls` 字段，并为 JSON/SSE 各补一个映射回归测试。

#### P1-4 取消接口可以把已完成/失败历史任务改成 cancelled

证据：`backend/app/api/evaluation.py::cancel_evaluation` 和 `backend/app/api/blind_test.py::cancel_blind_test` 不检查当前状态，收到任何任务 ID 就直接写 `cancelled`。

影响：已完成报告的 summary 可能仍残留或被前端隐藏，历史状态可被无意覆盖；取消语义不是幂等状态机。

修法建议：只允许 `pending/running` 转 `cancelled`；终态重复取消返回当前任务或 409；补 completed/failed/cancelled 三种状态的 API 测试。

#### P1-5 `RUN_EVAL_ON_CREATE=false` 时没有启动/重试任务 API

证据：设置项允许关闭创建即执行，但 `backend/app/api/evaluation.py` 只有 create/debug/get/logs/cancel，没有 `POST /evaluations/{id}/start` 或 retry。创建出的 pending 任务无法通过公开 API 启动。

影响：生产环境若关闭自动执行以等待队列/审批，任务会永久 pending；这与配置项语义不一致。

修法建议：补显式 start/retry，或删除该开关并接入持久化队列；定义重复 start、失败重试、幂等和权限。

#### P1-6 任务执行依赖进程内 daemon 线程，不能视为可靠后台队列

证据：`evaluation.py::_launch_evaluation` 和 `blind_test.py::_launch_blind_test` 使用 `threading.Thread(..., daemon=True)`；`main.py` 启动时只能把存活进程之外的 pending/running 标为 failed。

影响：进程重启会丢失运行状态，任务不能从 checkpoint 恢复；多 worker/多实例时 WebSocket、心跳字典和任务调度各自隔离；没有 durable queue、锁、重试和租约。

修法建议：短期增加显式 retry、任务 claim/lease 和取消标记；中期迁移 Celery/RQ/Arq/Redis 或数据库队列，保存节点/行级 checkpoint，WS 改为共享 pub/sub。

#### P1-7 SQLite 运行时补列不是数据库迁移方案

证据：`backend/app/core/database.py::ensure_runtime_schema` 通过 inspector + 多条 `ALTER TABLE` 只处理 SQLite；PostgreSQL 只依赖 `create_all`。README 路线图也承认 Alembic 尚未完成。

影响：新环境能启动不代表升级安全；多个进程同时启动可能竞争 DDL；生产 PostgreSQL 的已有库升级无版本、无回滚、无校验。

修法建议：引入 Alembic，建立初始 migration 和逐版本升级；保留 SQLite 本地兼容路径但让 CI 在空库和升级旧库两种场景跑迁移。

#### P1-8 Judge 指标分派存在缺失/重复，实验能力和产品能力边界不一致

证据：`build_metric()` 约 1207-1296 行；`builtin_citation_accuracy` 分支重复；`_is_llm_metric()` 没有 `CitationAccuracyMetric`；轨迹/恢复/工具选择三个类没有分派。

影响：手动创建这些 metric type 时可能无法创建、无法参与多采样/面板/换序；实验报告里的能力不能从 UI 复现。若以后直接暴露 Citation metric，engine prompt digest 也要重新检查是否覆盖其专用 Prompt。

修法建议：建立“MetricDefinition -> executor -> required fields -> UI layer -> test”注册表，删除重复分支；每个可暴露指标至少有 API 创建、场景选择、执行和报告回归测试。

### P2：规模、维护性和产品可信度

#### P2-1 默认场景/指标语义需要更严格的 schema 与失败策略

`_determine_pass()` 对没有 `pass_threshold` 的指标跳过通过判断；若自定义场景全部不设阈值，理论上任何行都可能被判通过，即使所有分数为 `None`。默认种子场景有 0.7 阈值，因此当前演示路径不一定触发。应在场景保存时要求至少一个有效阈值，或让 score error 永远导致 fail/unknown 而不是 pass。

#### P2-2 数据库外键与删除策略需实测并统一

`database.py` 只设置 SQLite WAL，没有显式 `PRAGMA foreign_keys=ON`；多个模型引用 Dataset/LLM/Endpoint，但资源删除 API 没有统一的“被任务引用时禁止删除/软删除”策略。需要用真实临时库验证删除数据集、删除 LLM、删除 endpoint 后历史报告是否可读；建议开启 FK、补索引和软删除/引用保护。

#### P2-3 endpoint 重试可能重复 POST 副作用，并且会重试 4xx

`invoke_endpoint()` 对除 `ValueError` 外的异常统一最多重试 3 次，`raise_for_status()` 抛出的 400/401/403 也会重试；请求是 POST，没有幂等键或“仅对可重试状态重试”策略。评测目标通常应是只读接口，但平台没有强制这一点。

#### P2-4 SSE 原始事件列表可能无界增长

`_invoke_sse()` 在流结束前持续 append `raw_events`，最后才截断拼接结果；超长流可能先把内存耗尽。应限制事件数/总字节数，并在 trace 中记录截断标记。

#### P2-5 seed-on-startup 会同步/覆盖默认配置和示例数据

`seed.py::sync_default_llm_config()` 每次启动都会用环境变量更新默认 LLM；`_upgrade_existing_*` 会更新预置指标/场景和示例报告。适合本地体验，但用户在 UI 修改默认配置后重启可能被覆盖；建议把 seed 与 migration 分离，并标记 demo 数据来源。

#### P2-6 示例完成报告是手工种子数据，缺少“演示数据”显式标识

`RAG 示例评测` 在 `seed.py` 中直接写入 completed、summary 和逐行分数，不是启动时真实调用 Judge 的产物。UI 若不标识，面试演示或用户可能误以为是真实实验结果。增加 `is_demo`/provenance 字段或 UI 标签。

#### P2-7 前端没有测试、类型边界松散、初始包过大

`frontend/src/services/api.ts` 及多个页面大量使用 `any`；CI 只有 `tsc -b && vite build`，没有组件测试、API contract 测试或 Playwright。静态路由导入导致首包约 1.9 MB。后续可先做 API 类型生成/共享 schema，再做路由 lazy loading 和关键流程 E2E。

#### P2-8 README 链接与当前 docs 不一致

README 中仍链接 `docs/usage-guide.md`、`docs/evaluation-guide.md`、`docs/database-and-storage.md` 等路径，但这些文件当前不存在；现有 docs 主要是中文分目录文档。由于用户要求本轮不以旧文档为实现依据，下一位应在不恢复旧删除文档的前提下重写 README 链接或建立当前文档索引。

## 7. 后续任务清单（建议顺序）

### 阶段 A：先完成证据闭环（下一位接手后优先）

- [ ] 在当前日期环境下对官方 FastAPI/Starlette、OpenAI-compatible API、SSE 规范、SQLAlchemy/Alembic、React Router/Axios 安全公告做联网核验，记录 URL、访问日期和适用版本。
- [ ] 复核报告第 1、3、4、5、6、9、12、13 节与项目的逐项映射，输出“已实现/部分实现/未实现”矩阵。
- [ ] 启动后端（使用 `conda activate eval` 或 `conda run -n eval`）和前端，验证 `/docs`、所有导航路由、种子数据、工作台加载。
- [ ] 不提交 Key 的前提下，用本地 mock HTTP JSON/SSE endpoint 完成一次离线评测、一次 endpoint 评测、一次报告复核、一次比较和一次盲测。
- [ ] 用 mock Judge 完成真实 `run_evaluation` 任务，确认 summary、cost、latency、WS/轮询和取消。
- [ ] 对 endpoint JSON/SSE 的 response/context/tool_calls 映射各写最小可复跑用例。
- [ ] 用临时 SQLite 库测试删除 Dataset/LLM/Endpoint 后历史任务、报告和外键行为；记录是否需要软删除。

### 阶段 B：先修结果正确性和状态机

- [ ] 修复 write-back 版本递增或改为独立输出表。
- [ ] 冻结完整 Judge/panel 配置并让 worker 使用快照实例；补 fingerprint 回归。
- [ ] 修复 SSE 结构化事件、顶层 tool_calls 接入 Agent 指标和完整 trace 截断。
- [ ] 限制取消只作用于 pending/running；补 start/retry API 和幂等状态转移。
- [ ] 明确任务失败/取消后部分结果的报告显示为 incomplete，不生成可误读的正常汇总。
- [ ] 建立 metric registry，接入或明确隐藏 Citation/Trajectory/Recovery/Tool Selection 等实验指标。

### 阶段 C：安全和可部署性

- [ ] 增加认证、RBAC、租户边界、审计事件和可信标注者身份。
- [ ] 增加 outbound endpoint allowlist、私网阻断、重定向限制、超时/响应大小限制。
- [ ] 对 LLM/API/Authorization 使用 Secret Manager 或加密存储；移除完整 Prompt/原始响应 INFO 日志。
- [ ] 引入 Alembic 和 PostgreSQL 升级测试；开启 SQLite 外键和必要索引。
- [ ] 将 daemon 线程替换为持久化队列/worker，增加 lease、重试、checkpoint 和多实例广播。
- [ ] 固化 Python 依赖（requirements lock 或 conda environment 文件），在 CI 使用同一版本矩阵。

### 阶段 D：产品闭环和面试证据

- [ ] 把人工标注的 Bad Case 一键加入回归数据集/标签切片。
- [ ] 从报告比较页支持“复制本次配置创建下一轮任务”，保留 baseline、版本和改动原因。
- [ ] 增加 release gate：阈值、错误率、延迟、成本和人工校准状态不满足时明确阻断。
- [ ] 为 demo 数据显示 provenance，为真实运行记录 run id、commit、模型版本、数据集版本和指标口径。
- [ ] 生成一张可脱敏架构图、一条真实 trace、一份评测表、一张 Bad Case 分类表和一次失败复盘，供面试使用；所有数字必须来自可复跑日志。
- [ ] 给前端补关键流程 E2E、API contract 类型和路由懒加载。

## 8. 建议下一位使用的工作方式

### 8.1 启动环境

```bash
cd /home/song/code/Agent/eval/Agent_Eval
conda activate eval
# 或在未激活 shell 中统一使用：conda run -n eval ...
```

后端：

```bash
cd backend
python -m pytest tests -q
python run.py
```

前端：

```bash
cd frontend
npm ci
npm run build
npm run dev
```

### 8.2 修改前检查

```bash
git status --short --branch
rg -n "目标符号|目标 API|目标字段" backend frontend
```

不要使用 `git reset --hard`、`git checkout --` 或批量删除来“清理”用户变更。不要恢复本次用户明确保留删除的旧文档，除非用户另行要求。

### 8.3 每个 Bug 的最低验证标准

1. 先写一个能在 `backend/tests/` 中失败的回归测试。
2. 修复最小范围代码，不顺手重构无关模块。
3. 用 `conda run -n eval python -m pytest tests -q` 全量回归。
4. 若涉及 UI/API，运行 `npm run build`，再做至少一个 API/UI 流程验证。
5. 在 handoff 或提交说明中记录“现象、根因、修复、测试和未覆盖边界”。

## 9. 适合面试的诚实项目叙述（当前版本）

可以这样描述：

> 我做的是一个面向 RAG、Agent 和多轮对话的 AI 评测工作台。用户先把样本、场景和判分标准固化下来，再选择 OpenAI-compatible Judge 或被测接口执行评测；平台按行保存分数、Judge 理由、接口 trace、token 成本和延迟，报告页支持失败样本定位、人工多人复核、kappa 校准、盲测和同口径回归比较。当前主链在单机可信环境已经跑通，评测执行仍是进程内 worker，认证、租户隔离、持久化任务队列和生产级 secret 管理还没有完成；它是评测平台，不是被测 Agent 本体。

面对追问时必须说明：

- 没有真实线上用户/QPS/收入指标就不要虚构。
- `docs/` 与 README 的历史数字需要回到代码、日志和可复跑实验核对。
- 轨迹忠实度、错误恢复、工具选择合理性、多模态和对抗 Judge 目前有研究代码/测试，但不能说已经是 Web 产品默认能力。
- Java/Spring 不是当前技术栈；若投 Java 后端岗位，需要单独准备 Java/JVM/Spring/MySQL/Redis/MQ/网络/算法，并把本项目只作为通用平台工程案例。

## 10. 交接完成判定

下一位 Agent 完成后，应至少更新本文件或新增后续 handoff，明确：

- 哪些 P0/P1 项已修复，关联 commit 和测试。
- 是否完成联网来源核验，列出来源与日期。
- 是否完成 mock JSON/SSE、真实 Judge（若有 Key）、浏览器和部署验证。
- 当前闭环是“本地可运行”“单机可交付”还是“可多人/生产部署”，不要使用含糊的“已完善”。
- 新的工作树、分支、远端提交状态，以及任何仍未提交的用户变更。
