.DS_Store

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/

# Local runtime data
.env
.env.*
!.env.example
backend/.env
backend/.env.*
!backend/.env.example
*.db
*.sqlite
*.sqlite3
*.db-wal
*.db-shm
*.db-journal
# 数据库备份：`*.db` 匹配不到 `eval_platform.db.bak-...`（后缀在 .db 之后），
# 而备份是真库的完整副本，含 llm_configs.api_key 明文 → 必须显式挡掉。
# 迁移前备份真库是既定流程（见 HANDOFF 5.8），所以这类文件会反复出现。
*.db.bak*
*.db.bak-*
*-backup.db*
backend/uploads/
backend/venv/
uploads/

# Frontend
frontend/node_modules/
frontend/dist/
frontend/tsconfig.tsbuildinfo

# IDE
.idea/
.vscode/
*.swp
*.swo

# Transient experiment artifacts
backend/ragas_comparison_checkpoint.json
